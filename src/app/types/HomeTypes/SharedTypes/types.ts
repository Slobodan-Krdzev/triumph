export type PromoDataType = {
  id: number,
  category: string,
  title: string;
  subFamilyType: string;
  desc: string;
  image: string;
  btnBlack: boolean;
};

export type ColorType = {
  colorName: string;
  price: number;
  image: string;
  colorCode: string;
};
